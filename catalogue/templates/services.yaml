apiVersion: v1
kind: Service

metadata:
  name: catalogue
  namespace: roboshop
  labels:
    project: roboshop
    environment: dev
    tier: backend
    component: catalogue

spec:
  selector:
    project: roboshop
    environment: dev
    tier: backend
    component: catalogue

  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
apiVersion: v1
kind: ConfigMap

metadata:
  name: catalogue
  namespace: roboshop
  labels:
    project: roboshop
    environment: dev
    tier: backend
    component: catalogue

data:
  MONGO: "true"
  MONGO_URL: {{ .Values.configMap.MONGO_URL }}